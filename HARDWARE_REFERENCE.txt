/**
 * STM32F103C8T6 BLE温湿度监控 - 硬件参考文档
 *
 * 本文档为前端开发者提供硬件配置参考
 */

/* ============================================
   硬件连接表
   ============================================

   JDY-18 BLE模块:
   ┌─────────────┬──────────────┬─────────────────┐
   │ JDY-18      │ STM32        │ 说明            │
   ├─────────────┼──────────────┼─────────────────┤
   │ TX          │ PB11 (UART3) │ 蓝牙发送→STM32  │
   │ RX          │ PB10 (UART3) │ STM32发送→蓝牙  │
   │ VCC         │ 3.3V         │ 供电(勿接5V!)   │
   │ GND         │ GND          │ 共地            │
   └─────────────┴──────────────┴─────────────────┘

   DHT11温湿度传感器:
   ┌─────────────┬──────────────┬─────────────────┐
   │ DHT11       │ STM32        │ 说明            │
   ├─────────────┼──────────────┼─────────────────┤
   │ DATA        │ PA0          │ 温湿度数据      │
   │ VCC         │ 3.3V/5V      │ 供电            │
   │ GND         │ GND          │ 共地            │
   └─────────────┴──────────────┴─────────────────┘

   调试串口:
   ┌─────────────┬──────────────┬─────────────────┐
   │ USB-TTL     │ STM32        │ 说明            │
   ├─────────────┼──────────────┼─────────────────┤
   │ RX          │ PA9 (UART1)  │ 调试输出        │
   │ 波特率      │ 115200       │                 │
   └─────────────┴──────────────┴─────────────────┘

   LED状态指示 (PC13):
   ┌─────────────┬──────────────┬─────────────────┐
   │ 状态        │ LED表现      │ 说明            │
   ├─────────────┼──────────────┼─────────────────┤
   │ 默认待机    │ 闪烁(500ms)  │ 未连接/未传输   │
   │ 正在传输    │ 常亮         │ 数据传输中      │
   │ 已停止      │ 常灭         │ 停止传输        │
   └─────────────┴──────────────┴─────────────────┘

*/

/* ============================================
   BLE UUID
   ============================================

   服务UUID: 0000ffe0-0000-1000-8000-00805f9b34fb
   特征UUID: 0000ffe1-0000-1000-8000-00805f9b34fb

   前端JavaScript连接代码:
   ```javascript
   const device = await navigator.bluetooth.requestDevice({
     acceptAllDevices: true,
     optionalServices: ['0000ffe0-0000-1000-8000-00805f9b34fb']
   });

   const server = await device.gatt.connect();
   const service = await server.getPrimaryService('0000ffe0-0000-1000-8000-00805f9b34fb');
   const characteristic = await service.getCharacteristic('0000ffe1-0000-1000-8000-00805f9b34fb');

   characteristic.addEventListener('characteristicvaluechanged', handler);
   await characteristic.startNotifications();
   ```

*/

/* ============================================
   命令协议
   ============================================

   前端发送命令:
   ┌───────────────┬──────────────┬───────────────┐
   │ 命令          │ STM32回复    │ 说明          │
   ├───────────────┼──────────────┼───────────────┤
   │ 1             │ START        │ 开始传输      │
   │ 0             │ STOP         │ 停止传输      │
   │ F:500         │ FREQ:500     │ 0.5秒间隔     │
   │ F:1000        │ FREQ:1000    │ 1秒间隔       │
   │ F:2000        │ FREQ:2000    │ 2秒间隔       │
   └───────────────┴──────────────┴───────────────┘

   STM32发送数据:
   ┌───────────────┬──────────────┬───────────────┐
   │ 数据          │ 含义         │ 格式          │
   ├───────────────┼──────────────┼───────────────┤
   │ T:25 H:60     │ 温度湿度     │ T:xx H:xx     │
   │ START         │ 开始确认     │ 固定字符串    │
   │ STOP          │ 停止确认     │ 固定字符串    │
   │ FREQ:xxx      │ 频率确认     │ FREQ:毫秒数   │
   │ ERR           │ DHT11错误    │ 固定字符串    │
   └───────────────┴──────────────┴───────────────┘

   模块自动发送(忽略):
   ┌───────────────┬──────────────┬───────────────┐
   │ +CONNECTED    │ 设备连接     │ 自动发送      │
   │ +DISCONNECTED │ 设备断开     │ 自动发送      │
   └───────────────┴──────────────┴───────────────┘

*/

/* ============================================
   前端代码示例
   ============================================

   发送命令:
   ```javascript
   const encoder = new TextEncoder('utf-8');
   await characteristic.writeValue(encoder.encode('1'));
   ```

   解析温湿度数据:
   ```javascript
   const match = text.match(/T:(\d+)\s*H:(\d+)/);
   if (match) {
     const temperature = parseInt(match[1]);
     const humidity = parseInt(match[2]);
   }
   ```

   设置频率:
   ```javascript
   await characteristic.writeValue(encoder.encode('F:1000'));
   ```

*/

/* ============================================
   浏览器兼容性
   ============================================

   支持的浏览器:
   - Chrome 70+ (Windows/Mac/Android) ✓
   - Edge 79+ (Windows/Mac) ✓
   - Opera 57+ ✓

   不支持的浏览器:
   - Safari (iOS完全不支持) ✗
   - Firefox ✗
   - 微信内置浏览器 ✗

   iOS限制说明:
   - 苹果出于安全考虑禁止Web Bluetooth API
   - iOS用户需要原生APP (React Native/Flutter)

*/

/* ============================================
   常见问题排查
   ============================================

   Q: 前端显示"浏览器不支持"?
   A: 使用Chrome浏览器，iOS Safari不支持

   Q: 电脑搜不到蓝牙设备?
   A: 1. 手机已连接会占用设备，先断开手机
      2. 给模块重新上电重启广播
      3. Windows BLE搜索慢，等待10秒以上

   Q: 连接后收不到数据?
   A: 1. 检查是否发送了 '1' 开始命令
      2. 检查UUID是否正确
      3. 查看串口调试输出

   Q: 显示"F:5001频率无效"?
   A: 命令合并问题，前端已添加100ms延迟
      STM32会自动处理，截取前3位

   Q: iPhone能用吗?
   A: 不能，Safari完全不支持Web Bluetooth
      需要开发React Native原生APP

*/